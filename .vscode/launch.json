{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "request": "launch",
            "name": "Launch Program",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}/my-app"
        }
    ]
}